Upload Project Model # Upload Project ModeldtModel = (await dtProject.getModel()).modelmodelFile = await uploadModel(os.path.dirname(__file__) + "/test_data/Drone.stmod", dtModel.data_dir, session)status = (await dtModel.setMainModelFile(modelFile=modelFile.getRef())).statusprint(status)